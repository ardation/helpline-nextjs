import React, { ReactElement } from 'react';
import Template from '.';

export default {
    title: 'Template',
};

export const Default = (): ReactElement => <Template text="Need to leave quickly?" />;
