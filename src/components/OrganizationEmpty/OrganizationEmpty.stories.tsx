import React, { ReactElement } from 'react';
import OrganizationEmpty from '.';

export default {
    title: 'OrganizationEmpty',
};

export const Default = (): ReactElement => <OrganizationEmpty />;
